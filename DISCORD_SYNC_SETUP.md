# Future Discord Sync

Discord-backed sync is intentionally paused. The current map stores tracking
data only in the visitor's browser, with no account, database, or external API
required.

When a shared backend is chosen, the browser records can be imported into the
account-based store without changing their timer behavior.
